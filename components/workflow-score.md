# Workflow Score Component

This file is a pointer. The canonical rubric lives at [`../reference/workflow-score.md`](../reference/workflow-score.md).

Load that file when scoring the reusable documentation workflow itself (separate from the project docs it improves). Use after every `doc-ratchet` or multi-project hardening run.
